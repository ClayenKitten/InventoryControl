<UserControl x:Class="InventoryControl.View.ProductDictionaryViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:vm="clr-namespace:InventoryControl.ViewModel"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:local="clr-namespace:InventoryControl.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext><vm:ProductDictionaryViewModel/></UserControl.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label BorderThickness="0, 0, 0, 1" BorderBrush="Black" Content="Прайс-лист"/>
        <TextBox Grid.Row="0" Grid.Column="1" mah:TextBoxHelper.Watermark="Поиск..."
            Style="{StaticResource SearchTextbox}" BorderThickness="1, 0, 0, 1"/>
        <DataGrid Grid.Row="1" Grid.ColumnSpan="2" ColumnHeaderStyle="{StaticResource DataGridColumnHeader}" ItemsSource="{Binding Content}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Артикул" MinWidth="100" Width="100" MaxWidth="100"/>
                <DataGridTextColumn Header="Наименование" MinWidth="100" Width="8*"/>
                <DataGridTextColumn Header="Кол" MinWidth="50" Width="1*"/>
                <DataGridTextColumn Header="Ед" MinWidth="50" Width="1*"/>
                <DataGridTextColumn Header="Закуп. цена" MinWidth="100" Width="2*"/>
                <DataGridTextColumn Header="Прод. цена" MinWidth="100" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
