<UserControl x:Class="InventoryControl.View.CounterpartyViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:local="clr-namespace:InventoryControl.View"
             xmlns:vm="clr-namespace:InventoryControl.ViewModel"
             xmlns:c="clr-namespace:InventoryControl.View.Controls"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext><vm:CounterpatyViewerVM/></UserControl.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Label BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,1,1" Content="{Binding Header}"/>
        <TextBox  x:Name="Searchbox" Grid.Column="1" mah:TextBoxHelper.Watermark="Поиск..."
                  Style="{StaticResource SearchTextbox}" BorderThickness="0,0,0,1"/>
        <c:AdvancedDataGrid Grid.Row="1" Grid.ColumnSpan="2" FilterString="{Binding ElementName=Searchbox, Path=Text}"
            ColumnHeaderStyle="{StaticResource DataGridColumnHeader}"
            ItemsSource="{Binding Content}" RowEditEnding="DataGrid_RowEditEnding">
            <c:AdvancedDataGrid.Columns>
                <DataGridTextColumn x:Name="LabelC" Header="Метка" Width="*" Binding="{Binding Id}"/>
                <DataGridTextColumn x:Name="NameC" Header="Наименование" Width="4*" Binding="{Binding Name}"/>
                <DataGridTextColumn x:Name="AddressC" Header="Юр. адрес" Width="2*" Binding="{Binding Address}"/>
                <DataGridTextColumn x:Name="PhoneC" Header="Телефон" Width="2*" Binding="{Binding Contacts}"/>
                <DataGridTextColumn x:Name="TaxpayerC" Header="ИНН" Width="2*" Binding="{Binding TaxpayerNumber}"/>
                <DataGridTextColumn x:Name="AccountC" Header="Расч. счёт" Width="2*" Binding="{Binding AccountingCode}"/>
                <DataGridTextColumn x:Name="BankIdC" Header="БИК" Width="1.5*" Binding="{Binding BankDetails}"/>
                <DataGridTemplateColumn x:Name="ConfirmC" Width="35" 
                    CanUserResize="False" CanUserSort="False" CanUserReorder="False">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <iconPacks:PackIconFontAwesome Kind="EditSolid" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </c:AdvancedDataGrid.Columns>
        </c:AdvancedDataGrid>
        <Grid Grid.Row="2" Grid.ColumnSpan="2">
            <Grid.Resources>
                <Style TargetType="TextBox" BasedOn="{StaticResource MahApps.Styles.TextBox}">
                    <Setter Property="BorderBrush" Value="Black"/>
                    <Setter Property="BorderThickness" Value="0,1,1,0"/>
                </Style>
            </Grid.Resources>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="{Binding ElementName=LabelC, Path=ActualWidth}"/>
                <ColumnDefinition Width="{Binding ElementName=NameC, Path=ActualWidth}"/>
                <ColumnDefinition Width="{Binding ElementName=AddressC, Path=ActualWidth}"/>
                <ColumnDefinition Width="{Binding ElementName=PhoneC, Path=ActualWidth}"/>
                <ColumnDefinition Width="{Binding ElementName=TaxpayerC, Path=ActualWidth}"/>
                <ColumnDefinition Width="{Binding ElementName=AccountC, Path=ActualWidth}"/>
                <ColumnDefinition Width="{Binding ElementName=BankIdC, Path=ActualWidth}"/>
                <ColumnDefinition Width="{Binding ElementName=ConfirmC, Path=ActualWidth}"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" mah:TextBoxHelper.Watermark="Метка..."/>
            <TextBox Grid.Column="1" mah:TextBoxHelper.Watermark="Наименование..."/>
            <TextBox Grid.Column="2" mah:TextBoxHelper.Watermark="Юридический адрес..."/>
            <TextBox Grid.Column="3" mah:TextBoxHelper.Watermark="Телефонный номер..."/>
            <TextBox Grid.Column="4" mah:TextBoxHelper.Watermark="ИНН..."/>
            <TextBox Grid.Column="5" mah:TextBoxHelper.Watermark="Расчётный счёт..."/>
            <TextBox Grid.Column="6" mah:TextBoxHelper.Watermark="БИК..."/>
            <Button Grid.Column="7" Style="{StaticResource MahApps.Styles.Button.Flat}"
                    Background="Transparent" BorderBrush="Black" BorderThickness="0,1,0,0">
                <iconPacks:PackIconFontAwesome Kind="PlusSolid" />
            </Button>
        </Grid>
    </Grid>
</UserControl>
